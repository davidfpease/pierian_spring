import React from 'react';

const CardHeader = (props) => {
  //need deck title 
  // card # of total cards
  // see all cards link
  return (
    <div>
      Card Header
    </div>
  )
}

export default CardHeader;